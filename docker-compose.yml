version: "3"
services:
        frrouter1:
        # Fix start command to not do this
                build:
                        context: ./build
                        dockerfile: Dockerfile-frrouter
                container_name: frrouter1
                image: frrouter-docker
                command: 'sh -c "/etc/init.d/frr start && sleep 1d"'
                cap_add:
                       - NET_ADMIN
        frrouter2:
                build:
                        context: ./build
                        dockerfile: Dockerfile-frrouter
                container_name: frrouter2
                image: frrouter-docker
                command: 'sh -c "/etc/init.d/frr start && sleep 1d"'
                cap_add:
                       - NET_ADMIN
